

let canvas = document.getElementById("canvas");
let g = canvas.getContext("2d")

g.fillstyle = "#2e6ede"; 
g.fillrect(0, 0, canvas.clientWidth, canvas.clientHeight);

g.fillstyle = "#leca07";
g.fillstyle(0, 0, 10, 10);
